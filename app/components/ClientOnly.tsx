// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
import { ClientOnly as RUClientOnly } from 'remix-utils/client-only';

// Wrap the library to keep maintenance on this file only.
export const ClientOnly = RUClientOnly;
