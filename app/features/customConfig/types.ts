export enum OneClickEnvironment {
  Sandbox = 'sandbox',
  Production = 'production',
}

export enum OneClickContentTitle {
  Signup = 'Signup',
  Signin = 'Signin',
  Verify = 'Verify',
  Apply = 'Apply',
}

export enum OneClickContentVerificationOptions {
  OnlyCode = 'only_code',
  OnlyLink = 'only_link',
  BothLinkAndCode = 'both_link_and_code',
}
